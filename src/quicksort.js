module.exports =  {
	quickSort : function (arr) {
		var length = arr.length()
		, pivot = arr[length / 2]
		, i = 0
		, f = length-1;

		while (i < j) {
			while (i)

		}

		if (pivot ) {}


		return [1,2,3,4,5,6,7,8,9];
	},
};
